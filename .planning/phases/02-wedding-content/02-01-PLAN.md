---
phase: 02-wedding-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/App/Scripts/Managers/GroupData.cs
autonomous: true
requirements:
  - PERS-01
  - PERS-02
  - PERS-03

must_haves:
  truths:
    - "GroupData has hasApartment, apartmentName, apartmentAddress, and apartmentNotes fields"
    - "Developer can fill per-group apartment details in Unity Inspector on the App GameObject"
    - "hasBreakfastPref field is unchanged — no serialization breakage"
  artifacts:
    - path: "Assets/App/Scripts/Managers/GroupData.cs"
      provides: "Serializable group data model with apartment fields"
      contains: "hasApartment"
  key_links:
    - from: "GroupData.hasApartment"
      to: "AlloggioPopup.OnShowing()"
      via: "App.Instance.CurrentGroup.hasApartment"
      pattern: "hasApartment"
---

<objective>
Extend GroupData with four apartment-specific fields needed by AlloggioPopup (Plan 04) to show per-group accommodation details.

Purpose: The AlloggioPopup needs to know whether a group has a reserved apartment and, if so, its name, address, and any notes. GroupData is the single source of truth for all per-group data (set in Unity Inspector by the couple).
Output: GroupData.cs with four new fields; all existing fields untouched.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-wedding-content/02-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Current GroupData.cs — executor reads before modifying -->
From Assets/App/Scripts/Managers/GroupData.cs:
```csharp
[Serializable]
public class GroupData
{
    #region Inspector Variables
    public string password = "";
    public string groupDisplayName = "";
    public List<string> memberNames = new List<string>();
    public bool hasBreakfastPref = false;
    #endregion
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add apartment fields to GroupData</name>
  <files>Assets/App/Scripts/Managers/GroupData.cs</files>
  <read_first>
    - Assets/App/Scripts/Managers/GroupData.cs — current state, must preserve existing fields verbatim
  </read_first>
  <action>
Add four new fields inside the existing `#region Inspector Variables` block, AFTER `hasBreakfastPref`. Do NOT reorder or touch existing fields.

Fields to add:
```csharp
[Tooltip("True for groups with a reserved apartment. Controls AlloggioPopup display.")]
public bool hasApartment = false;

[Tooltip("Apartment display name shown in AlloggioPopup, e.g. 'Appartamento Gialli'.")]
public string apartmentName = "";

[Tooltip("Full address of the apartment, e.g. 'Via Napoli 12, Città'.")]
public string apartmentAddress = "";

[Tooltip("Extra notes displayed in AlloggioPopup: check-in time, key pickup instructions, etc. One note per list entry.")]
public List<string> apartmentNotes = new List<string>();
```

No other changes. `using` statements at the top already include `System`, `System.Collections.Generic`, and `UnityEngine` — no new usings needed.
  </action>
  <verify>
    <automated>grep -n "hasApartment\|apartmentName\|apartmentAddress\|apartmentNotes" Assets/App/Scripts/Managers/GroupData.cs</automated>
  </verify>
  <done>
    GroupData.cs contains all four new fields with [Tooltip] attributes. All existing fields (password, groupDisplayName, memberNames, hasBreakfastPref) still present and unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inspector → Runtime | GroupData values are set by the couple in Unity Inspector — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | GroupData.apartmentAddress | accept | Address is wedding-day logistics info shared with guests — not sensitive PII; stored in Unity asset, not transmitted |
</threat_model>

<verification>
After task completes:
- `grep hasApartment Assets/App/Scripts/Managers/GroupData.cs` returns a match
- `grep apartmentNotes Assets/App/Scripts/Managers/GroupData.cs` returns a match
- `grep hasBreakfastPref Assets/App/Scripts/Managers/GroupData.cs` still returns a match (existing field unchanged)
</verification>

<success_criteria>
GroupData.cs compiles (no syntax errors) and Unity Inspector on App GameObject shows the four new fields in the Group Database list entries.
</success_criteria>

<output>
After completion, create `.planning/phases/02-wedding-content/02-01-SUMMARY.md`
</output>
